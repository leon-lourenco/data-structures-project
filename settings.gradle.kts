rootProject.name = "data-structures-project"

// Modules are added incrementally, one per commit, as each structure is implemented.
include(
    "linear:dynamic-array",
    "hashing:hash-table",
    "trees:binary-search-tree",
    "linear:linked-list",
    "linear:stack",
)
