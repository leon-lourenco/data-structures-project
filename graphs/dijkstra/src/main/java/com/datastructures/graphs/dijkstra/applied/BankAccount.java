package com.datastructures.graphs.dijkstra.applied;

/** A correspondent-bank account, identified by bank code and account id — a graph node. */
public record BankAccount(String bankCode, String accountId) {
}
